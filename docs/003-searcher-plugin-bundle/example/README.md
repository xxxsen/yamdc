# 示例说明

本目录提供搜索插件 bundle 的最小示例。

包含：

1. `manifest.yaml`
   - 演示 bundle 元信息
   - 演示 `category + name` 组链配置
   - 演示默认链和分类链

2. `config.json`
   - 演示主程序如何声明 plugin bundle source
   - 演示远程 source 与本地 source 的组合方式

这些示例只展示 bundle 与配置层语义，不包含具体站点插件逻辑。
