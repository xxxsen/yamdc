version: v1
matchers:
  # override 层可以按规则名覆盖同名系统规则
  - name: generic_fc2
    category: FC2
    uncensor: true
    pattern: '(?i)FC2[-_\\s]?([0-9]{4,})'
    normalize_template: 'FC2-PPV-$1'
    score: 120
    require_boundary: false

