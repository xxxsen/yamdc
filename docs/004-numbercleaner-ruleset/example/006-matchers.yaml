matchers:
  # 一个最小示例：命中主番号后同时推导 category 和 uncensor
  - name: generic_fc2
    category: FC2
    uncensor: true
    pattern: '(?i)FC2[-_\\s]?(?:PPV)?[-_\\s]?([0-9]{3,})'
    normalize_template: 'FC2-PPV-$1'
    score: 100
    require_boundary: false

