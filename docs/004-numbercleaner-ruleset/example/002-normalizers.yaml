normalizers:
  # 取文件基础名
  - name: basename
    type: builtin
    builtin: basename

  # 去扩展名
  - name: strip_ext
    type: builtin
    builtin: strip_ext

  # 统一大写
  - name: to_upper
    type: builtin
    builtin: to_upper

